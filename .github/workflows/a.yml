on:
  # push:
  workflow_dispatch:
    inputs:
      drive:


env:
  keys_password: ${{ secrets.password }}
  drive_copy: ${{ secrets.drive_copy }}
  drive_trash: ${{ secrets.drive_trash }}


jobs:

  copy-drive:
    runs-on: ubuntu-latest

    steps:
      - uses: fu1ny2t-3es/go1gl2-dr3ve@erase-drive
        with:
          token: ${{ secrets.token }}
          drive: ${{ inputs.drive }}
